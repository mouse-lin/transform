gp for mouse
use schema-less to sql db
#----------------------------------------------
Transform:
   #外部主要调用的模块
   create_table!: TableCreator::create_table!
   drop_table!(table_name): TableCreator::drop_table!

TableCreator: 
  create_table!:  创建表
  drop_table!:  删除表

Attributes:
  Attributes数据格式:  字段 => Attribute对象(@klass,@name,@type,@default_value)

#----------------------------------------------
#TODO  1.后创建索引表时候，需要重新插入新的一些数据;  2. 分布式存取 3. 重构代码  优先级： 1、3、2
# PPT演示准备 、数据库数据准备
#  视屏录制，这个看情况需要不需要； 周四过去尝试投影
